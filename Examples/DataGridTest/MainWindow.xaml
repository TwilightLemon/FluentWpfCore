<Window x:Class="DataGridTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:fluent="https://github.com/TwilightLemon/FluentWpfCore"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Smooth DataGrid Virtualization Test"
        Width="1180"
        Height="720"
        MinWidth="760"
        MinHeight="480"
        mc:Ignorable="d">
    <Window.Resources>
        <!--
            DataGrid normally creates its own ScrollViewer. This template replaces that
            internal scrolling host with SmoothScrollViewer while retaining the named
            parts expected by DataGrid and ScrollViewer.
        -->
        <Style x:Key="SmoothDataGridStyle" TargetType="{x:Type DataGrid}">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#FFD5D9E0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HorizontalGridLinesBrush" Value="#FFE8EBF0" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#FFF7F9FC" />
            <Setter Property="RowHeight" Value="34" />
            <Setter Property="ColumnHeaderHeight" Value="38" />
            <Setter Property="HeadersVisibility" Value="All" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGrid}">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <fluent:SmoothScrollViewer x:Name="DG_ScrollViewer"
                                                       CanContentScroll="True"
                                                       Focusable="False"
                                                       HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                                       IsEnableSmoothManipulating="True"
                                                       IsEnableSmoothScrolling="True"
                                                       VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                <fluent:SmoothScrollViewer.Physics>
                                    <fluent:DefaultScrollPhysics Smoothness="0.55" />
                                </fluent:SmoothScrollViewer.Physics>
                                <fluent:SmoothScrollViewer.Template>
                                    <ControlTemplate TargetType="{x:Type fluent:SmoothScrollViewer}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <Button Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                    Command="{x:Static DataGrid.SelectAllCommand}"
                                                    Focusable="False"
                                                    Visibility="{Binding HeadersVisibility, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.All}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />

                                            <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter"
                                                                            Grid.Column="1"
                                                                            Visibility="{Binding HeadersVisibility, Converter={x:Static DataGrid.HeadersVisibilityConverter}, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />

                                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                                    Grid.Row="1"
                                                                    Grid.ColumnSpan="2"
                                                                    CanContentScroll="{TemplateBinding CanContentScroll}" />

                                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                                       Grid.Row="1"
                                                       Grid.Column="2"
                                                       Maximum="{TemplateBinding ScrollableHeight}"
                                                       Orientation="Vertical"
                                                       Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />

                                            <Grid Grid.Row="2" Grid.Column="1">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="{Binding NonFrozenColumnsViewportHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <ScrollBar x:Name="PART_HorizontalScrollBar"
                                                           Grid.Column="1"
                                                           Maximum="{TemplateBinding ScrollableWidth}"
                                                           Orientation="Horizontal"
                                                           Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                           ViewportSize="{TemplateBinding ViewportWidth}"
                                                           Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                                            </Grid>
                                        </Grid>
                                    </ControlTemplate>
                                </fluent:SmoothScrollViewer.Template>

                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </fluent:SmoothScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Background" Value="#FFF0F3F8" />
            <Setter Property="BorderBrush" Value="#FFD5D9E0" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>

        <Style TargetType="{x:Type DataGridCell}">
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock FontSize="24"
                           FontWeight="SemiBold"
                           Text="Smooth DataGrid" />
                <TextBlock Margin="0,4,0,0"
                           Foreground="#FF606A78"
                           Text="Pixel scrolling · Recycling virtualization · SmoothScrollViewer" />
            </StackPanel>
            <Border Grid.Column="1"
                    Padding="12,6"
                    VerticalAlignment="Center"
                    Background="#FFE8F1FF"
                    CornerRadius="12">
                <TextBlock x:Name="ItemCountText"
                           Foreground="#FF245C9F"
                           FontWeight="SemiBold" />
            </Border>
        </Grid>

        <DataGrid x:Name="TestDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  EnableColumnVirtualization="True"
                  EnableRowVirtualization="True"
                  FrozenColumnCount="1"
                  IsReadOnly="True"
                  SelectionUnit="FullRow"
                  Style="{StaticResource SmoothDataGridStyle}"
                  VirtualizingPanel.CacheLength="2"
                  VirtualizingPanel.CacheLengthUnit="Page"
                  VirtualizingPanel.IsContainerVirtualizable="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTextColumn Width="80" Binding="{Binding Id}" Header="ID" />
                <DataGridTextColumn Width="130" Binding="{Binding OrderNumber}" Header="Order" />
                <DataGridTextColumn Width="160" Binding="{Binding OrderDate, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Header="Date" />
                <DataGridTextColumn Width="190" Binding="{Binding Customer}" Header="Customer" />
                <DataGridTextColumn Width="220" Binding="{Binding Product}" Header="Product" />
                <DataGridTextColumn Width="120" Binding="{Binding Category}" Header="Category" />
                <DataGridTextColumn Width="90" Binding="{Binding Quantity}" Header="Quantity" />
                <DataGridTextColumn Width="120" Binding="{Binding UnitPrice, StringFormat={}{0:C2}}" Header="Unit price" />
                <DataGridTextColumn Width="130" Binding="{Binding Total, StringFormat={}{0:C2}}" Header="Total" />
                <DataGridTextColumn Width="120" Binding="{Binding Status}" Header="Status" />
                <DataGridTextColumn Width="120" Binding="{Binding City}" Header="City" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
